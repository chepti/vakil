name: Deploy to Hostinger

# הפעלה ידנית בלבד — Hostinger חוסם את כתובות ה-IP של GitHub Actions,
# כך שה-SSH נכשל ב-timeout. הפריסה היומיומית נעשית מקומית עם deploy.ps1.
# מי שרוצה לנסות פריסה דרך GitHub: Actions -> Deploy to Hostinger -> Run workflow.
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # public/build מנוהל ב-git, לכן git pull מביא גם את הקוד וגם את ה-assets הבנויים.
      # אין צורך ב-npm build / scp — רק משיכה בשרת וסנכרון ל-public_html.
      - name: Deploy via SSH (pull code + assets, sync, clear caches)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/domains/vakil.chepti.com
            git checkout -- public/build 2>/dev/null || true
            git clean -fd public/build
            git pull origin main
            cp -rf public/. public_html/
            /opt/alt/php83/usr/bin/php artisan migrate --force
            /opt/alt/php83/usr/bin/php artisan config:clear
            /opt/alt/php83/usr/bin/php artisan route:clear
            /opt/alt/php83/usr/bin/php artisan view:clear
